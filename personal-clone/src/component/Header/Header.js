import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <div>
        <h1>Header</h1>
        <button>Login</button>
        <button>Cart</button>
      </div>
    );
  }
}

export default Header;
